[pytest]
pythonpath = src .
testpaths = tests
markers =
    uses_news_cache: opt out of autouse empty news_feed cache for correlation tests
    integration: optional integration tests (skipped in CI without credentials)
    breeze_live: live Breeze API checks; requires BREEZE_API_KEY, BREEZE_SECRET, BREEZE_SESSION_TOKEN
